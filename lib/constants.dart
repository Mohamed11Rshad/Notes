import 'dart:ui';

import 'package:flutter/material.dart';

Color kPrimaryColor = const Color(0xff62FCD7);
String kNotesBox = "notes_box";

List<Color> kColors = [
  const Color(0xffcccccc), // Light taupe (muted and neutral) - Base color
  const Color(0xfff5f5dc), // Light beige (subtle and warm) - Keep for warmth
  const Color(0xff005073), // Dark teal (rich and sophisticated)
  const Color(0xff9b59b6), // Plum purple (deep and vibrant)
  // Very light cool grey (muted and cool)
  const Color(0xfff0e68c), // Light peach (soft and cheerful)
  const Color(0xffbae6ff), // Light blue (soft and calming)
  const Color(0xffffa500), // Light orange (energetic and vibrant)
];
